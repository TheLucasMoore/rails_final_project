<%= form_for recipe_comments do |f| %>

  <%= f.label "Add Your Comment" %><br>
  <%= f.text_area :content %><br>

  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :recipe_id %>

<%= f.submit %>
<% end %>